#Exactly same as ones and zeros function only difference is, it provides flexibility to provide our own number to fill the array.
import numpy as np

a1 = np.full(shape=(2, 3, 4), fill_value=22)
print(a1)